@model IEnumerable<CoffeeShop.Models.Pedido>

@{
    ViewData["Title"] = "Pedidos";
}

<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="display-4 text-brown">Nossos Pedidos</h1>
        <a asp-action="Create" class="btn btn-primary btn-lg shadow-sm">
            <i class="fas fa-plus-circle"></i> Criar Novo Pedido
        </a>
    </div>

    <div class="row g-4">
        @foreach (var item in Model)
        {
            <div class="col-md-6 col-lg-4">
                <div class="card h-100 shadow-sm hover-card">
                    <div class="card-body">
                        <h5 class="card-title text-primary">
                            <i class="fas fa-receipt me-2"></i> Pedido - @item.DataEmissao.ToShortDateString()
                        </h5>
                        <ul class="list-unstyled">
                            <li class="mb-2">
                                <i class="fas fa-user text-muted me-2"></i> Cliente: @item.Cliente.CPF
                            </li>
                            <li class="mb-2">
                                <i class="fas fa-box text-muted me-2"></i> Produto: @item.Produto.Nome
                            </li>
                            <li class="mb-2">
                                <i class="fas fa-dollar-sign text-muted me-2"></i> Valor Total: @item.ValorTotal.ToString("C")
                            </li>
                        </ul>

                        <div class="d-flex gap-2 mt-3">
                            <a asp-action="Edit" asp-route-id="@item.Id"
                               class="btn btn-outline-primary btn-sm flex-fill">
                                <i class="fas fa-edit"></i> Editar
                            </a>
                            <a asp-action="Details" asp-route-id="@item.Id"
                               class="btn btn-outline-info btn-sm flex-fill">
                                <i class="fas fa-info-circle"></i> Detalhes
                            </a>
                            <a asp-action="Delete" asp-route-id="@item.Id"
                               class="btn btn-outline-danger btn-sm flex-fill"
                               onclick="return confirm('Tem certeza que deseja deletar este pedido?');">
                                <i class="fas fa-trash"></i> Deletar
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</div>

<style>
    .text-brown {
        color: #6F4E37;
    }

    .btn-primary {
        background-color: #6F4E37;
        border-color: #6F4E37;
    }

        .btn-primary:hover {
            background-color: #5D4037;
            border-color: #5D4037;
        }

    .hover-card {
        transition: transform 0.2s ease-in-out;
    }

        .hover-card:hover {
            transform: translateY(-5px);
        }

    .text-primary {
        color: #6F4E37 !important;
    }

    .list-unstyled li {
        display: flex;
        align-items: center;
    }

    .btn-outline-primary:hover {
        background-color: #6F4E37;
        color: #fff;
    }

    .btn-outline-info:hover {
        background-color: #17a2b8;
        color: #fff;
    }

    .btn-outline-danger:hover {
        background-color: #dc3545;
        color: #fff;
    }
</style>

@section Scripts {
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" />
}
